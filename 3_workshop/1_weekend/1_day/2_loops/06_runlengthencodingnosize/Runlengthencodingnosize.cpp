#include <soi>
#include <map>

signed main() {
  int n = read_int();
  vector
}

// std::map<int, int> m;
//   int n = read_int();
//   int a;
//   for(int i = 1; i <= n; i++) {
//     a = read_int();
//     m.count(a) == 0 ? m[a] = 1 : ++m[a];
//   }

//   for(std::map<int, int>::iterator iter = m.begin(); iter != m.end(); ++iter) {
//     std::cout << iter->first << " " << iter->second << "\n";
//   }